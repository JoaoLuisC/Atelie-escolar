# react-helmet-async@2 ainda declara peer react<=18, mas o projeto roda em
# React 19 (funciona normalmente). Sem isto, `npm install`/`npm ci` (inclusive
# na CI) abortam com ERESOLVE. Remover quando react-helmet-async publicar peer
# range compatível com React 19.
legacy-peer-deps=true
